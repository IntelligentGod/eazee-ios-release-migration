/** Same `subtract` as `MarchingCubes.addBall` in `BlobMesh` field build. */
export const BLOB_METABALL_SUBTRACT = 11;

/** Iso level where the implicit surface is extracted (was `m.isolation` on marching cubes). */
export const BLOB_METABALL_ISO = 56.8;
